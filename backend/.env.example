# Backend environment example for Customer Management Portal
# Copy to .env and replace placeholders before running in production.

# --- PostgreSQL Connection ---
DB_HOST=localhost
DB_PORT=5432
DB_USER=cmp_user
DB_PASSWORD=replace_me
DB_DATABASE=cmp_db

# --- JWT ---
# Use a long, random string. Rotate if leaked.
JWT_SECRET=qk&dcX3LZp-2KgG

# --- Optional Feature Overrides ---
# Leave unset for auto-detect (matview enabled only if exists & unique index present)
# USE_USER_STATUS_MATVIEW=true
# USE_USER_STATUS_MATVIEW=false

# Verbose HTTP request logging (disabled by default)
# VERBOSE_HTTP_LOG=1

# Build metadata (set by CI, optional)
# BUILD_TIMESTAMP=2025-11-09T00:00:00Z

# If running behind a reverse proxy with HTTPS termination, set NODE_ENV=production
NODE_ENV=production
