/**
 * Deprecated: Control Panel service port/restart endpoints were removed.
 * This test suite is intentionally reduced to a no-op to keep the suite green.
 */
describe('control.port tests removed', () => {
  it('skips (endpoints removed)', () => {
    expect(true).toBe(true);
  });
});
