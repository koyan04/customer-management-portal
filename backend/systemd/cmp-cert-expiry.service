[Unit]
Description=CMP Certificate Expiry Check
After=network.target

[Service]
Type=oneshot
WorkingDirectory=/srv/cmp/backend
Environment=NODE_ENV=production
ExecStart=/usr/bin/node scripts/check_cert_expiry.js ${DOMAIN_NAME}
User=root

[Install]
WantedBy=multi-user.target
